# 25 Warehouse 10.289

Decision: proposed shortage result is shown inside the shortage detail card, not as a separate status in the open list.

MVP:
- do not add separate open-list status for proposal;
- show proposal inside shortage detail card;
- send notification about status/event change to the related warehouse keeper;
- keep open list compact.
