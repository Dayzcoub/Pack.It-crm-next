# 25 Warehouse 10.287

Decision: if the assigned user suggests write-off, they must provide a reason.

MVP:
- write-off suggestion requires a reason field;
- the reason is used by warehouse keeper, admin, or director for the final decision;
- save suggested result and reason in history;
- final write-off still requires allowed role confirmation.
