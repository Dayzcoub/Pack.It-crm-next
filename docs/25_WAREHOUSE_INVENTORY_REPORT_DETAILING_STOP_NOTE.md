# 25 Warehouse — Inventory Report Detailing Stop Note

## 10.190. Stop detailing the inventory report

Decision: the inventory report has enough detail for the current warehouse documentation stage.

Already fixed:
- the inventory report is generated manually by button when needed;
- it is an internal document only;
- no prices are included by default;
- the main report shows discrepancies only;
- a full checked-list checkpoint can be generated only when needed;
- discrepancies are grouped by equipment category;
- the report records who conducted the inventory;
- attachment thumbnails are not shown in the report by default;
- a compact summary is shown at the top;
- generated reports are stored in history;
- no versioning or outdated status is required for inventory reports.

Decision on full checkpoint export:
- do not add more detail now;
- the optional full-check checkpoint is enough for MVP;
- further export layout can be refined later if needed.

Next documentation area:
- move to the internal warehouse problem/loss report.
