# Warehouse 10.224

Decision: after confirmation, the item first goes to returned/check state. It can be used again only after inspection.

Rules:
- confirmation does not make the item usable immediately;
- first target state is returned / check;
- technician completes inspection;
- next status depends on inspection result;
- all actions are logged.
